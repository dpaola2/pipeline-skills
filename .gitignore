# Project work is externalized — see pipeline.md Work Directory
# These directories should not exist in the pipeline repo
projects/
inbox/

# Pipeline configs — obsolete (skills now run from target repo)
# Kept for safety in case any local copies still exist
pipeline.md

# Dashboard
dashboard/node_modules/

# Claude Code local settings
.claude/settings.json
